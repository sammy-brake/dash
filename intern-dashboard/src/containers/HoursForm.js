
import React, { Component } from 'react';

class HoursForm extends Component {

    render() {
    return (
        <div className="hours">
            <h1>Update Hours</h1>
                {/* {interns.map(intern => 
                  <div key={intern.id} className="intern-text" >
                      <ul>
                   <form> 
                       {/* or, do I pass the id of the intern to the onChange function that I call? I think I do that */}
                      {/* <label>{intern.name}: {intern.hours_worked}/{intern.hours_allowed}  </label>
                      <input type="number"/> */}
                      {/* <input type="hidden" value={intern.id} ></input> */}
                    {/* </form> */}
                   {/* </ul> */}
                 </div>
                 )};
        // </div>
    // );
 };
// };


export default HoursForm;